// Front-end bundle entry point, compiled by esbuild to assets/js/main.js and
// enqueued with the `defer` strategy, so the DOM is ready by the time it runs.
import { initNavigation } from './modules/navigation.js';

initNavigation();
